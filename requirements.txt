fastapi>=0.110.0
uvicorn>=0.28.0
a2wsgi>=1.10.0
pyyaml>=6.0.1
pydantic>=2.6.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
pytest>=8.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
pyjwt>=2.8.0
python-multipart>=0.0.9
anyascii>=0.3.2
# EUR-Lex : extraction depuis le PDF officiel archive quand le HTML d'un acte
# est inaccessible — la piece justificative opposable aux regulateurs. Le code
# reste tolerant a son absence (l'acte est alors signale en echec), mais la
# dependance est installee d'office pour que le repli fonctionne partout.
pypdf>=4.0.0
# openpyxl>=3.1.0 # Optional, uncomment to read the XLSX form of the Australian DFAT list
# pyspark>=3.5.0 # Optional, uncomment for running distributed Spark batch jobs

